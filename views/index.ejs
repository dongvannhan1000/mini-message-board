<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1><%= title %></h1>
    
    <% messages.forEach((message, index) => { %>
        <div class="message">
            <p><span class="user"><%= message.user %>:</span> <%= message.text %></p>
            <p class="date">Added on: <%= message.added.toLocaleString() %></p>
            <a href="/message/<%= index %>" class="open-btn">Open</a>
        </div>
    <% }); %>

    <a href="/new" class="new-message-link">Add a new message</a>
</body>
</html>